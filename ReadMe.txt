The script is designed for parsing car sales ads from the site av.by.

To parse a specific brand/model, set the search_url variable to a URL,
which is generated when choosing the selection parameters on the site av.by.

Results are sorted by price in US dollars.

Parsing results are written to csv and json files.